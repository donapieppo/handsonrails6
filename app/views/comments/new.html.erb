<%= main_title('Aggiungi un commento') %>

<%= simple_form_for [@game, @comment] do |comment| %>  
  <%= comment.input :name, label:false, as: :text %>  
  <%= comment.submit 'Invia', class: 'btn btn-primary btn-sm'  %>  
<% end %>
